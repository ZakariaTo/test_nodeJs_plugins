pipeline {
    agent any
    stages {
        stage("WORSKAPCE") {
            steps {
            node {
                echo "WORSKAPCE : ${env.WORKSPACE}"
                }
            }
        }
    }
}
